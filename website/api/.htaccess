Options -Indexes
<FilesMatch "^(config|config\.local|backend)\.php$|submissions\.json|.*\.sqlite$">
Require all denied
</FilesMatch>
<IfModule mod_headers.c>
Header set Cache-Control "no-cache, no-store, must-revalidate"
Header set Pragma "no-cache"
Header set Expires "0"
</IfModule>
