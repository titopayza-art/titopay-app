TitoPay deployment build — 2026-08-13 UTC
Branch: claude/peach-payments-403-fix-6sv7cy
PWA bundle: v331 (titopay-pwa-v331)
Admin console: admin-console-v81

This build — FICA collects typed identifiers, not only uploads:

  - Personal: the FICA screen now asks for the ID number (13-digit SA ID,
    checked against the ID checksum so a typo is caught immediately) or
    the passport/permit number — the label follows the chosen document
    type — plus the residential address, all BEFORE the uploads.
  - Business: the responsible person's ID/passport number, the CIPC
    company registration number (e.g. 2020/123456/07) and the business
    address, alongside the existing CIPC document upload requirement.
  - The server validates and stores the typed values on the review, and
    the Admin Compliance Queue now renders them legibly (document kind +
    number, company registration, address, file names) so the reviewer
    checks the documents AGAINST the declared details.

Full API suite 459/459 (FICA tests extended to the new contract: missing
typed ID number is refused; typed values land on the review row).

Migrations to run: none.

Operational reminders (unchanged):
  node scripts/ensure-revenue-wallet.js --create   (once, on production)
  npm install after extracting api.zip (node-forge dependency)
  Email Centre SMTP + titopay-email-worker must be running for mail delivery.
