TitoPay deployment build — 2026-08-12 UTC
Branch: claude/peach-payments-403-fix-6sv7cy
PWA bundle: v315 (titopay-pwa-v315)
Admin console: admin-console-v76

This build:
  - Free ticket purchase no longer requires the platform revenue wallet
    (a free ticket charges no fee, so it never touches the fee-collection
    wallet). Fixes "buying the tickets refuses" for free events.
  - Ticketing and Ticketing Staff are now two distinct screens:
      Ticketing        — organiser hub: create/manage events, sell, tags, sales.
      Ticketing Staff  — door scanner: pick the event, scan tickets, watch the
                         running attendance count. No management clutter.
  - New read-only endpoint GET /v1/ticketing/business/events/:id/attendance,
    gated by the same "scan" permission the scanner uses, so the door sees the
    count before the first scan (organiser and assigned scanners alike).

Migrations to run: none new in this build.

Operational check (important): confirm the single platform revenue wallet
exists in production, or PAID ticketing and top-up fees cannot be recorded:
  node scripts/ensure-revenue-wallet.js            (report only)
  node scripts/ensure-revenue-wallet.js --create   (provision if missing)
