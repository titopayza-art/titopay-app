TitoPay deployment build — 2026-08-13 UTC
Branch: claude/peach-payments-403-fix-6sv7cy
PWA bundle: v329 (titopay-pwa-v329)
Admin console: admin-console-v80

This build — staff are told what they were added to do:

  - Adding an event ticket scanner now notifies the person immediately:
    an in-app alert AND an email naming the event, what they can do
    (scan tickets at the gate, watch attendance — no money movement, no
    FICA needed) and exactly where to find it (Event Tickets > Scan
    entry on their own profile). Removing them sends an in-app alert so
    the scanner disappearing is never a mystery.
  - The Staff register under Business tools is unchanged in behaviour
    (device-local preview) but now says plainly that people on it are
    NOT notified — and that event scanners added under Ticketing are.
  - The Event Tag issue panel wording is now plain language: write each
    code to its tag now, this is the only time they are shown; TitoPay
    keeps no readable copy, so an unwritten code means reissuing that
    tag. (Same behaviour as before — only the explanation changed. This
    panel is only ever shown to the organiser issuing tags, not to the
    public.)
  - Also in api.zip from earlier today: Event Tag references are now 10
    plain digits, unique per event (new tags only).

Verified live against a real database: staff add -> one in-app alert +
one email with the event name; staff remove -> one in-app alert.
Full API suite 459/459.

Migrations to run: none.

Operational reminders (unchanged):
  node scripts/ensure-revenue-wallet.js --create   (once, on production)
  npm install after extracting api.zip (node-forge dependency)
  Email Centre SMTP + titopay-email-worker must be running for mail delivery.
