TitoPay deployment build — 2026-08-13 UTC
Branch: claude/peach-payments-403-fix-6sv7cy
PWA bundle: v330 (titopay-pwa-v330)
Admin console: admin-console-v80

This build — Staff is real, for day-to-day operations:

  BUSINESS SIDE (Business tools > Staff — the Preview badge disappears):
  - The register is server truth: synced to the business account across
    devices (endpoints /v1/business/staff the app was already wired for).
  - Adding a person with their TitoPay @username / email / phone links
    their account and notifies them in the app AND by email, naming the
    role and explaining exactly what they can and cannot do. Removing
    notifies too. A contact with no TitoPay match is still saved on the
    register, honestly labelled: not notified, cannot sell yet.
  - Fixed on the way: the register's own Remove button had been broken by
    the ticketing staff-remove handler swallowing its action.

  STAFF SIDE (personal profile > My Workplaces — new):
  - Staff see every business that added them, with their role.
  - "Take a sale": tap the business's products (or type an amount), show
    the payment QR. The QR is minted with the BUSINESS's identity, so the
    customer's payment lands in the business wallet directly — staff never
    hold a cent. Basket priced server-side; tracked stock counts down;
    oversell asks for explicit confirmation.
  - Every till sale is logged under the staff member's name and appears in
    the business's Sales > Staff report (till sales + amounts alongside
    door scans).

Proven live (verification/business-staff-live.js, 9/9): add -> linked +
alert + email; workplace visible; basket sale priced R60 with stock
10 -> 8; the QR provably belongs to the business; the performance report
credits the cashier; typed-amount sales; strangers refused; removal turns
selling off and is announced; unlinked entries honestly labelled.
Full API suite 459/459.

Migrations to run: none — new tables create themselves on first use
(also in schema.sql for fresh installs).

Operational reminders (unchanged):
  node scripts/ensure-revenue-wallet.js --create   (once, on production)
  npm install after extracting api.zip (node-forge dependency)
  Email Centre SMTP + titopay-email-worker must be running for mail delivery.
