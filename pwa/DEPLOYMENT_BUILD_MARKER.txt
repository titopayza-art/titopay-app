TitoPay deployment build — 2026-08-13 UTC
Branch: claude/peach-payments-403-fix-6sv7cy
PWA bundle: v332 (titopay-pwa-v332)
Admin console: admin-console-v81

This build — approved FICA details appear on statements:

  - Once (and only once) a FICA review is APPROVED, statements carry the
    verified identity:
      PDF statement (Activity > Download PDF statement): the account
      details block shows the verified ID number (or, for a business, the
      CIPC registration number) in a compressed right-hand column, and the
      FICA-verified address replaces the profile address.
      Email statement: a "FICA-VERIFIED ACCOUNT HOLDER" block at the top —
      name, ID/passport number, CIPC registration for businesses, and
      address — above the movement lines.
  - Gating proven live: an approved review yields the details; flipping
    the same review back to pending yields nothing. A pending or rejected
    submission never decorates a document.
  - New endpoint GET /v1/kyc/fica/verified-details (own account only).

Full API suite 459/459.

Migrations to run: none.

Operational reminders (unchanged):
  node scripts/ensure-revenue-wallet.js --create   (once, on production)
  npm install after extracting api.zip (node-forge dependency)
  Email Centre SMTP + titopay-email-worker must be running for mail delivery.
