TitoPay Admin build: admin-console-v84-20260815

This build:
  - New Platform page "Security Content" (/security-content/): edits the
    security warning and safety tips shown inside the customer app. Reads
    GET /admin/security-content, writes PUT /admin/security-content, stored
    in platform_settings under "security_content" and audit-logged.
  - Friendly form with a repeating tips editor (title, body, icon select)
    and a live customer preview beside it. The icon list is restricted to
    the glyphs the customer app can draw.
  - No field ever saves blank: clearing one restores its built-in wording.
    A failed read shows the built-in copy with a warning rather than
    silently overwriting stored content on the next save.
  - Visible to admins holding the "security" permission.

Upload notes:
  - Asset stamp moves from admin-console-v83 to admin-console-v84 across all
    66 HTML files, so operators cannot be served a cached admin.js without
    the new route.
  - Ships a new directory: security-content/index.html.
  - Needs the API build carrying the security-content endpoints.
